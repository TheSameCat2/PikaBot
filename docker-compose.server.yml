services:
  palbot:
    image: ${PALBOT_IMAGE:-ghcr.io/your-github-user/palbot:main}
    container_name: palbot
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - ./data:/data
